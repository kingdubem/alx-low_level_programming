#include <stdio.h>

/**
 * main - Entry point
 * Return: Always 0 (Success/correct)
 */
int main(void)
{
int d;
for (d = '0'; d <= '0'; d++)
{
putchar(d);
{
putchar(',');
putchar(',');
}
}
putchar('\n);
return (0);
}
